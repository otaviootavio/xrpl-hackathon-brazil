import React from 'react'
import ReactDOM from 'react-dom/client'
import Formulario from './App.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Formulario />
  </React.StrictMode>,
)
