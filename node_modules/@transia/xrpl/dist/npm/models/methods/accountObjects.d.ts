import { LedgerIndex } from '../common';
import { Check, DepositPreauth, Escrow, Hook, Offer, PayChannel, RippleState, SignerList, Ticket, URIToken } from '../ledger';
import { BaseRequest, BaseResponse } from './baseMethod';
type AccountObjectType = 'check' | 'deposit_preauth' | 'escrow' | 'hook' | 'nft_offer' | 'offer' | 'payment_channel' | 'signer_list' | 'state' | 'ticket' | 'uritoken';
export interface AccountObjectsRequest extends BaseRequest {
    command: 'account_objects';
    account: string;
    type?: AccountObjectType;
    deletion_blockers_only?: boolean;
    ledger_hash?: string;
    ledger_index?: LedgerIndex;
    limit?: number;
    marker?: unknown;
}
type AccountObject = Check | DepositPreauth | Escrow | Hook | Offer | PayChannel | SignerList | RippleState | Ticket | URIToken;
export interface AccountObjectsResponse extends BaseResponse {
    result: {
        account: string;
        account_objects: AccountObject[];
        ledger_hash?: string;
        ledger_index?: number;
        ledger_current_index?: number;
        limit?: number;
        marker?: string;
        validated?: boolean;
    };
}
export {};
//# sourceMappingURL=accountObjects.d.ts.map