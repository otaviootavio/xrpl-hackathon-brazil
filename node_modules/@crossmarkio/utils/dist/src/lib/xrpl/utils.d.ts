import * as Big from 'big.js';
import * as xrpl from 'xrpl';
import { Amount } from 'xrpl/dist/npm/models/common';
declare function normalizeNodes(metadata: xrpl.TransactionMetadata): any[];
declare function parseCurrencyAmount(currencyAmount: Amount): string | {
    currency: string;
    issuer: string;
    value: string;
};
declare function getAffectedAccounts(metadata: xrpl.TransactionMetadata): string[];
declare function parseQuality(bookDirectory: string, pays: string, gets: string): Big.Big;
declare const parseEscrowID: (id: string, account: string) => boolean;
export { normalizeNodes, parseEscrowID, parseCurrencyAmount, getAffectedAccounts, parseQuality };
