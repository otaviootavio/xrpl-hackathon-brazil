"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Storage = void 0;
const modules_1 = require("../../modules");
const typings_1 = __importDefault(require("@crossmarkio/typings"));
const Config = typings_1.default.crossmark.config.Config.config;
class Storage {
    Modules;
    browser;
    constructor(opts) {
        this.Modules = new modules_1.Modules(opts);
        this.browser = this.Modules.browser;
    }
    get = async (key) => {
        if (this.browser)
            return (await this.browser.storage.local.get(key))[key];
        let data = localStorage.getItem(key);
        if (data)
            return JSON.parse(data);
        return;
    };
    set = async (key, value) => {
        let data = Object.fromEntries([[key, value]]);
        if (this.browser) {
            await this.browser.storage.local.set(data);
            return await this.browser.storage.local.get(key);
        }
        return localStorage.setItem(key, JSON.stringify(value));
    };
    remove = async (key) => {
        try {
            if (this.browser)
                await this.browser.storage.local.remove(key);
            return localStorage.removeItem(key);
        }
        catch (e) {
            return;
        }
    };
}
exports.Storage = Storage;
//# sourceMappingURL=index.js.map