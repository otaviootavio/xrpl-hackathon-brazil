declare class Config {
    static config: Config.config;
}
declare namespace Config {
    interface config {
        run: 'development' | 'production' | 'test';
        env: 'chrome' | 'firefox' | 'opera';
        version: string;
        init: {
            background: boolean;
            content: boolean;
        };
        dev: boolean;
        beta: boolean;
        logger: boolean;
        key: string;
        credentials: {
            xumm: {
                key: string;
            };
        };
    }
}
export { Config };
