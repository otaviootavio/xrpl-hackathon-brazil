import { Extension } from '../../extension/index';
import { NetworkEvent } from './network';
import { UserEvent } from './user';
import { StateEvent } from './state';
import { SignoutEvent } from './signout';
declare namespace Events {
    interface BaseEvent {
        id: string;
        type: Extension.TYPES;
        target: Extension.TARGETS;
    }
    interface GenericEvent extends BaseEvent {
        event: Extension.EVENTS;
    }
    type EventMessage = GenericEvent | NetworkEvent | UserEvent | StateEvent | SignoutEvent;
}
declare class Events {
    static GenericEvent: Events.GenericEvent;
    static BaseEvent: Events.BaseEvent;
    static EventMessage: Events.EventMessage;
    static NetworkEvent: NetworkEvent;
    static UserEvent: UserEvent;
    static StateEvent: StateEvent;
    static SignoutEvent: SignoutEvent;
}
export { Events };
