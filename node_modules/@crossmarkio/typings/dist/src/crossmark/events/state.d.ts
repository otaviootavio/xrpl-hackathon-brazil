import { Extension } from '../../extension/index';
import { Events } from '../events';
import { latest } from '../../schema/latest';
export interface StateEvent extends Events.BaseEvent {
    event: Extension.EVENTS.STATE_UPDATE;
    data: {
        state: typeof latest.Types.Api.Types.StateSchema;
    };
}
