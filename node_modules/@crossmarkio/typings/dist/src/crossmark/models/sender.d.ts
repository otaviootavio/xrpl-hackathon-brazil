/// <reference types="chrome" />
import { BaseFullResponse, BaseRequest, BaseResponse } from '../models/common/base';
import { Extension } from '../../extension/index';
export interface SenderRequest extends BaseRequest {
    command: Extension.COMMANDS.SENDER;
    data: {
        id: string;
    };
}
interface SenderDataResponse {
    data: {
        sender: chrome.runtime.MessageSender;
    };
}
export type SenderResponse = BaseResponse & SenderDataResponse;
export interface SenderFullResponse extends BaseFullResponse {
    request: SenderRequest;
    response: SenderResponse;
}
export {};
