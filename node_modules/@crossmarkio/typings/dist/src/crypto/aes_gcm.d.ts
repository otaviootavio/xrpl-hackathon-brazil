import * as forge from 'node-forge';
import { PBKDF2 } from './pbkdf2';
import { AlgoTypes } from './algoTypes';
interface EncryptResponse {
    bytes: string;
    hex: string;
}
interface DencryptResponse {
    bytes: string;
    hex: string;
    data: string;
}
declare class AES_GCM {
    algo: forge.cipher.Algorithm;
    type: AlgoTypes;
    iv: string;
    tag: string;
    derivation: PBKDF2;
    encrypt: ({ key, data }: {
        key: string;
        data: string;
    }) => Promise<EncryptResponse>;
    decrypt: ({ key, hex }: {
        key: string;
        hex: string;
    }) => Promise<DencryptResponse>;
}
export { AES_GCM };
