import { AES_CBC, AES_GCM, Standard, PBKDF2 } from '../../../crypto/index';
import { AlgoTypes } from '../../../crypto/algoTypes';
declare class Security {
    static EncryptTypes: Security.EncryptTypes;
    static AlgoTypes: typeof AlgoTypes;
}
declare namespace Security {
    type EncryptTypes = AlgoTypes;
    const AllEncryptionTypes: {
        AES_CBC: typeof AES_CBC;
        AES_GCM: typeof AES_GCM;
        PBKDF2: typeof PBKDF2;
        Standard: typeof Standard;
    };
    type EncryptionTypes = AES_CBC | AES_GCM | PBKDF2 | Standard;
    type PasscodeEncryptionTypes = Standard | PBKDF2;
    type CardEncryptionTypes = AES_CBC | AES_GCM;
}
export { Security };
